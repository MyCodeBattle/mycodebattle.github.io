---
categories: Posts
date: 2014-09-01 00:00:00
title: UVa 1267 - Network
tags: []
layout: post
---

#  [UVa 1267 - Network](/2014/09/UVa-1267/ "UVa 1267 - Network")

By [MyCodeBattle](http://mycodebattle.gitcafe.io/about "MyCodeBattle")

Published Sep 2 2014 16:11

**Contents**

  1. 1. 传送门
  2. 2. 题意
  3. 3. 思路
  4. 4. 代码

## 传送门

[UVa 1267 - Network](http://www.bnuoj.com/v3/problem_show.php?pid=18406)

## 题意

一个服务器最多覆盖范围k，求放置最少的服务器覆盖所有叶子。

## 思路

按距离存入叶子，从距离最大的叶子开始向上数k个祖先放置服务器。把主服务器当成根。

## 代码
    
    
    123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081

| 
    
    
    #include <cstdio>#include <cstring>#include <vector>#define LL long long#define lowbit(x) ((x) & (-x))const int MAXN = 1000 + 5;const int INF = 0x3f3f3f3f;using namespace std; vector<int> mp[MAXN], nodes[MAXN];int pa[MAXN], covered[MAXN], k, n; void Build(int cur, int f, int d){    pa[cur] = f;    int no = mp[cur].size();    if (no == 1 && d > k)        nodes[d].push_back(cur);    for (int i = 0; i < no; i++)    {        int v = mp[cur][i];        if (v != f) Build(v, cur, d + 1);    }} void DFS(int cur, int f, int d){    covered[cur] = 1;    for (int i = 0; i < mp[cur].size(); i++)    {        int v = mp[cur][i];        if (v != f && d < k)            DFS(v, cur, d + 1);    }} int Solve(){    memset(covered, 0, sizeof covered);    int ans = 0;    for (int d = n; d > k; d--)    {        for (int i = 0; i < nodes[d].size(); i++)        {            int t = nodes[d][i];            if (covered[t]) continue;            int v = t;            for (int j = 0; j < k; j++)                v = pa[v];            DFS(v, -1, 0);            ans++;        }    }    return ans;} int main(){    //freopen("input.txt", "r", stdin);    int T, i, j, s;    scanf("%d", &T);    while (T--)    {        scanf("%d%d%d", &n, &s, &k);        for (int i = 1; i <= n; i++)        {            mp[i].clear();            nodes[i].clear();        }        for (i = 0; i < n - 1; i++)        {            int a, b;            scanf("%d%d", &a, &b);            mp[a].push_back(b);            mp[b].push_back(a);        }        Build(s, -1, 0);        printf("%d\n", Solve());    }    return 0;}  
  
---|---  
  
[Solving Reports](/categories/Solving-Reports/)

[Online Judge - UVa](/tags/Online-Judge-UVa/)[Foundation - Greedy](/tags/Foundation-Greedy/)
